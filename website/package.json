{
  "name": "flyradar",
  "version": "1.0.0",
  "description": "Manage your Fly.io resources in style.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "watch": "npx @tailwindcss/cli -i ./priv/input.css -o ./priv/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./priv/input.css -o ./priv/output.css",
    "pagefind": "npx pagefind --source ./priv",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.7",
    "prettier": "^3.5.1",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "tailwindcss": "^4.0.7",
    "pagefind": "^1.3.0"
  },
}
